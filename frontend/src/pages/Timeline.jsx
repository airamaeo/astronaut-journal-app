// Fetches data from backend /api/nasa/timeline?dob=YYYY-MM-DD
// Displays series of timelineCards & handles loading & error states

export default function Timeline() {
    return <h2>This is the Timeline page</h2>;
}  